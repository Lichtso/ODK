/**
 DirectionalLight
 @class Can't be constructed. Objects are created by loading containers.
 @extends LightObject
*/
function DirectionalLight(){
	
	/**
	 Can set the bounds via a Vector3,
     or get the bounds as a cloned instance.
     @param {Matrix4} [bounds] The new bounds vector
     @returns {Matrix4} A copy of the bounds vector
	*/
	this.bounds = function(){
		//[native code]
	}

}